{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the Optical Table window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
